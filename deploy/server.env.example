# [EN] Copy this file to deploy/server.env and fill in the real values.
# deploy/server.env is in .gitignore, never committed (the real IP
# and server URL are not for git, so they don't leak into the repository).
# [RU] Скопируйте этот файл в deploy/server.env и впишите реальные значения.
# deploy/server.env — в .gitignore, никогда не коммитится (реальный IP
# и адрес сервера — не для git, чтобы не утекли в репозиторий).

SERVER_IP=203.0.113.10
SITE_URL=https://203-0-113-10.sslip.io/
